- name: Pull frontend image
  docker_image:
    name: my-frontend
    source: pull

- name: Run frontend container
  docker_container:
    name: frontend
    image: my-frontend:latest
    state: started
    restart_policy: always
    ports:
      - "80:80"
