- name: Deploy Backend to AKS
  hosts: localhost
  connection: local

  tasks:
    - name: Apply backend deployment
      kubernetes.core.k8s:
        state: present
        src: "{{ playbook_dir }}/../../k8s/backend.yaml"
