- name: Deploy Ingress Rules
  hosts: localhost
  connection: local

  tasks:
    - name: Apply ingress
      kubernetes.core.k8s:
        state: present
        src: "{{ playbook_dir }}/../ingress.yaml"
        namespace: default
